import Button from "../../Button/Button";

function ButtonReceived() {
  const handleUpdateOrder = () => {};
  return (
    <Button action onClick={handleUpdateOrder}>
      Đã nhận hàng
    </Button>
  );
}

export default ButtonReceived;
